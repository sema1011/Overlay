--- a/global.cpp
+++ b/global.cpp
@@ -1,3 +1,6 @@
+#include <leptonica/allheaders.h>
+#include <leptonica/pix.h>
+#include <leptonica/pix_internal.h>
 #include <QMutex>
 #include <QBuffer>
 #include <QDir>
--- a/zmangaview.cpp
+++ b/zmangaview.cpp
@@ -11,6 +11,7 @@
 #include <QThreadPool>
 #include <QElapsedTimer>
 #include <QComboBox>
+#undef emit
 #include <algorithm>
 #include <execution>
 #include <functional>
--- a/zfilecopier.cpp
+++ b/zfilecopier.cpp
@@ -54,7 +54,7 @@
             Q_EMIT progressClose();
 
             if (m_abort) {
-                Q_EMIT errorMsg(tr("Copying aborted, %1 files was copied.").arg(m_filesCopied));
+                Q_EMIT errorMsg(tr("Copying aborted, %1 files was copied.").arg(int(m_filesCopied)));
             } else if (!m_errors.empty()) {
                 Q_EMIT errorMsg(tr("Failed to copy %1 entries:\n%2")
                                 .arg(m_errors.count())
--- a/readers/zpdfreader.cpp
+++ b/readers/zpdfreader.cpp
@@ -93,7 +93,7 @@
         return false;
     }
 
-    GooString fname(fileName.toUtf8());
+    GooString fname(fileName.toUtf8().constData());
     m_doc = PDFDocFactory().createPDFDoc(fname);
 
     if (m_doc==nullptr || !m_doc->isOk())
@@ -263,7 +263,7 @@
         const quint8 cBlue = 0xff;
         const quint8 cAlpha = 0;
         SplashColor bgColor { cRed, cGreen, cBlue, cAlpha };
-        SplashOutputDev splashOutputDev(splashModeXBGR8, 4, false, bgColor, true);
+        SplashOutputDev splashOutputDev(splashModeXBGR8, 4, bgColor, true);
         splashOutputDev.setFontAntialias(true);
         splashOutputDev.setVectorAntialias(true);
         splashOutputDev.setFreeTypeHinting(true, false);
--- a/qmanga.desktop
+++ b/qmanga.desktop
@@ -5,5 +5,5 @@
 Icon=Alien9
 GenericName=QManga book reader
 Terminal=false
-Categories=Application;Graphics;Viewer;
+Categories=Graphics;Viewer;
 MimeType=application/x-cbz;application/x-cbr;image/jpeg;image/png;image/gif;image/bmp;application/x-zip;application/zip;application/x-rar;application/pdf;image/vnd.djvu
--- a/qmanga.pro
+++ b/qmanga.pro
@@ -98,6 +98,7 @@
 CONFIG += warn_on link_pkgconfig c++17
 
 LIBS += -lz -ltbb
+DEFINES += TBB_USE_CPPLIB=1
 
 # for deprecations in GCC 11-12 without oneTBB
 DEFINES += TBB_SUPPRESS_DEPRECATED_MESSAGES
@@ -117,6 +118,7 @@
         error("Dependency error: icu not found.");
     }
 
+    !contains(CONFIG, portage) {
     packagesExist(poppler-cpp) {
         packagesExist(poppler) {
             CONFIG += use_poppler
@@ -144,6 +146,10 @@
         CONFIG += use_epub
         message("Using libepub:        YES")
     } else {
+        exists(/usr/lib64/libepub.so) {
+            CONFIG += use_epub
+            message("Using libepub:        YES")
+        } else {
         exists(/usr/local/lib/libepub.so) {
             CONFIG += use_epub
             message("Using libepub:        YES")
@@ -151,25 +157,26 @@
             message("Using libepub:        NO")
         }
     }
+    }
 
-    use_poppler {
+    contains(CONFIG, use_poppler) {
         DEFINES += WITH_POPPLER=1
         PKGCONFIG += poppler-cpp poppler
     }
 
-    use_ocr {
+    contains(CONFIG, use_ocr) {
         DEFINES += WITH_OCR=1
         QMAKE_CXXFLAGS += -Wno-ignored-qualifiers
         PKGCONFIG += tesseract
-        LIBS += -llept
+        LIBS += -lleptonica
     }
 
-    use_djvu {
+    contains(CONFIG, use_djvu) {
         DEFINES += WITH_DJVU=1
         PKGCONFIG += ddjvuapi
     }
 
-    use_epub {
+    contains(CONFIG, use_epub) {
         DEFINES += WITH_EPUB=1
         LIBS += -lepub
     }
@@ -188,7 +195,7 @@
     DEFINES += WITH_OCR=1
     QMAKE_CXXFLAGS += -Wno-ignored-qualifiers
     PKGCONFIG += tesseract
-    LIBS += -llept
+    LIBS += -lleptonica
 
     DEFINES += WITH_DJVU=1
     PKGCONFIG += ddjvuapi
@@ -212,10 +219,6 @@
 TARGET       = qmanga
 TARGET.files = qmanga
 TARGET.path  = $$INSTALL_PREFIX/bin
-INSTALLS    += TARGET desktop icons
+INSTALLS    += TARGET 
 
-desktop.files	= qmanga.desktop
-desktop.path	= $$INSTALL_PREFIX/share/applications
 
-icons.files	= img/Alien9.png
-icons.path	= $$INSTALL_PREFIX/share/icons
