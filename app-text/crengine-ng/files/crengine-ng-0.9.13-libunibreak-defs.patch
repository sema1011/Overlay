--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -378,7 +378,9 @@ if (LIBUNIBREAK_FOUND)
     endif()
     set(USE_LIBUNIBREAK ON)
     list(APPEND PRIVATE_INCLUDE_DIRECTORIES ${LIBUNIBREAK_INCLUDE_DIR})
     list(APPEND PRIVATE_LINK_LIBRARIES ${LIBUNIBREAK_LIBRARIES})
+    list(APPEND PRIVATE_COMPILE_DEFINITIONS -DUSE_LIBUNIBREAK=1)
 else()
     set(USE_LIBUNIBREAK OFF)
+    list(APPEND PRIVATE_COMPILE_DEFINITIONS -DUSE_LIBUNIBREAK=0)
 endif()
